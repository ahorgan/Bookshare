<%= form_for(@share) do |f| %>
  <% if @share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share.errors.count, "error") %> prohibited this share from being saved:</h2>

      <ul>
      <% @share.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :from_user_id, :value => current_user.id %>
  </div>
  <div class="field">
    <%= f.label('Add User') %>
    <%= f.select(:to_user_id, options_for_select(@users)) %>
  </div>
  <div class="field">
    <%= f.label('Add Book') %>
    <%= f.select(:book_id, options_for_select(@books)) %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
